# lazy.nvim clones itself on the first start, see lua/gordon/lazy.lua.
# Run one headless sync, so a new machine gets every plugin at once.
- name: Sync the nvim plugins
  ansible.builtin.command:
    argv:
      - nvim
      - --headless
      - "+Lazy! sync"
      - +qa
  changed_when: true
