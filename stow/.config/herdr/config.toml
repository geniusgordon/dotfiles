onboarding = false
# herdr configuration
# Defaults: `herdr --default-config`. Validate: `herdr config check`.

[ui]
agent_panel_sort = "spaces"

# Create a tab at once, like tmux. Rename it later with prefix+shift+t.
prompt_new_tab_name = false

[keys]
# Match the old tmux prefix. herdr takes ctrl+s in every pane, so the zsh
# `herdr_workspacizer` widget only fires outside herdr. Use prefix+f inside.
prefix = "ctrl+s"

# Workspace picker. prefix+s runs the fzf popup below, so the native picker
# keeps the herdr default of prefix+w. Settings moves off prefix+s.
settings = "prefix+shift+s"

# Detach, to match tmux. This replaces the herdr default of prefix+q.
detach = "prefix+d"

# Splits, to match the old tmux bindings.
split_vertical = "prefix+|"
split_horizontal = "prefix+minus"

# Pane focus, vim direction keys.
focus_pane_left = "prefix+h"
focus_pane_down = "prefix+j"
focus_pane_up = "prefix+k"
focus_pane_right = "prefix+l"

# Workspace order navigation. The ctrl variant means the workspace level,
# to match prefix+p and prefix+n for tabs.
previous_workspace = "prefix+ctrl+p"
next_workspace = "prefix+ctrl+n"

# Tab motion. herdr has no move-tab action, so this is order navigation only.
previous_tab = "prefix+p"
next_tab = "prefix+n"

# Workspace picker, the old tmux `bind s` fzf popup. It lists the open
# workspaces and focuses the selected one.
[[keys.command]]
key = "prefix+s"
type = "popup"
command = "herdr-workspace-picker"
width = "60%"
height = "50%"

# Project picker, the old tmux `bind C-s display-popup "tmux-sessionizer"`.
# herdr reserves prefix+prefix to send a literal prefix, so this uses prefix+f.
# HERDR_NO_ATTACH stops the script from starting a second TUI inside the popup.
[[keys.command]]
key = "prefix+f"
type = "popup"
command = "HERDR_NO_ATTACH=1 herdr-workspacizer"
width = "80%"
height = "80%"
